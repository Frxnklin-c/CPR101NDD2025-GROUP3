// Header guard to prevent multiple inclusions of this header file
#ifndef CONVERTING_H
#define CONVERTING_H

// Standard libraries required by the converting module
#include <stdio.h>     // For input and output functions like printf
#include <string.h>    // For string manipulation functions like strlen and strcmp
#include <stdlib.h>    // For the atoi function (string to int conversion)

// Declaration of the converting function (defined in converting.c)
void converting(void);

// End of header guard
#endif

