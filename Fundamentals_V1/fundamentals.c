#include <stdio.h>      // Allows the use of input/output functions.
#include "fundamentals.h"       // Includes the header file containing the fundamentals() declaration.

// This is the entry point of the program.
int main (){

fundamentals();     // Calls the fundamentals() function to run the logic from fundamentals.h file.
    return 0;       // Tells the operating system that the program finished successfully.
}
